import { DISABLE_SPEEDY } from "styled-components/dist/constants"
import { useFarms } from "../../components/hooks/useFarms"

const useHomePgaeData = () => {
    const [homeData, setHomedata] = useState()
    const farms = useFarms()
    const fiels = useFields()

    If (famr.) {

    }

    dasdas


    DISABLE_SPEEDY
    DataTransfer
return {
    farms,
    fields,
    
}
}